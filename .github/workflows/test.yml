name: Aresmandi

env:
  TZ: Asia/Shanghai

  # ── 内核源码 ──
  KERNEL_NAME: Ares-4.14-KVM-Proton-release
  KERNEL_REPO: https://github.com/dawood333/kernel_mt6893
  KERNEL_BRANCH: test
  KERNEL_DEVICE: ares
  DEFCONFIG_NAME: ares_defconfig

  # ── 工具链 ──
  # Mandi-Sa LLVM 23.0.0 (Binutils 2.45.1)

  # ── AnyKernel3 ──
  ANYKERNEL_REPO: https://github.com/osm0sis/AnyKernel3
  ANYKERNEL_BRANCH: master

  # ── 构建参数 ──
  ARCH: arm64
  OUT_DIR: "${{ github.workspace }}/out"

  # ── KBUILD 伪装配置 ──
  FAKE_EXTRAVERSION: ${{ inputs.fake_extraversion }}
  FAKE_COMPILER: ${{ inputs.fake_compiler }}

  # ── ccache ──
  CCACHE_KEY: ccache-ares-4.14-llvm23

on:
  workflow_dispatch:
    inputs:
      ksu_enabled:
        description: '是否集成 ReSukiSU (Manual Hook)'
        required: true
        type: boolean
        default: true

      droidspaces_enabled:
        description: '是否集成 Droidspaces 支持（应用官方补丁并写入所需内核配置）'
        required: true
        type: boolean
        default: true

      use_anykernel:
        description: '是否使用 AnyKernel3 打包刷入包（关闭时仅上传 Image）'
        required: true
        type: boolean
        default: true

      fake_enabled:
        description: '是否进行编译伪装（总开关；开启后各伪装项留空即保留真实值）'
        required: true
        type: boolean
        default: false
        
      ccache_enabled:
        description: '是否启用 ccache 加速编译'
        required: true
        type: boolean
        default: true

      ccache_update:
        description: '更新 ccache 缓存（更改编译配置或需要刷新缓存时开启）'
        required: true
        type: boolean
        default: false

      release:
        description: '是否发布到 GitHub Releases'
        required: true
        type: boolean
        default: true

      kernel_suffix:
        description: '自定义内核后缀（留空使用默认，开头勿加连字符）'
        required: false
        type: string
        default: ''

      kbuild_build_user:
        description: '伪装编译用户名 (KBUILD_BUILD_USER)，留空则保留真实值'
        required: false
        type: string
        default: ''

      kbuild_build_host:
        description: '伪装编译主机名 (KBUILD_BUILD_HOST)，留空则保留真实值'
        required: false
        type: string
        default: ''

      kbuild_build_timestamp:
        description: '伪装编译时间 (KBUILD_BUILD_TIMESTAMP)，留空则保留真实值'
        required: false
        type: string
        default: ''

      kbuild_build_version:
        description: '伪装编译版本 (KBUILD_BUILD_VERSION)，留空则保留真实值'
        required: false
        type: string
        default: '1'

      fake_extraversion:
        description: '伪装内核版本号后缀 (FAKE_EXTRAVERSION)，留空则保留真实值'
        required: false
        type: string
        default: ''

      fake_compiler:
        description: '伪装编译器信息 (FAKE_COMPILER)，留空则保留真实值'
        required: false
        type: string
        default: ''

jobs:
  # ============================================================
  #  构建内核
  # ============================================================
  build:
    name: 构建内核
    runs-on: ubuntu-22.04
    steps:

      # ── 1. 初始化构建环境 ──
      - name: 初始化构建环境
        id: setup_env
        run: |
          echo "builddate=$(date +'%Y%m%d')" >> $GITHUB_OUTPUT

          sudo sed -i '/azure.archive.ubuntu.com/d' /etc/apt/apt-mirrors.txt
          sudo apt-get update
          sudo apt-get install -y build-essential bc gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi \
            libssl-dev libfl-dev curl git ccache python2 python2-dev zip unzip p7zip-full

      # ── 2. 克隆内核源码 ──
      - name: 克隆内核源码
        run: |
          git clone --recursive --depth=1 -j $(nproc) \
            --branch ${{ env.KERNEL_BRANCH }} \
            ${{ env.KERNEL_REPO }} \
            ${{ env.KERNEL_NAME }}

      # ── 3. 获取工具链 (LLVM 23 Mandi-Sa) ──
      - name: 获取工具链
        run: |
          mkdir -p aosp-clang
          CLANG_URL="https://github.com/Mandi-Sa/clang/releases/download/amd64-kernel-arm_static-23/llvm23.0.0-binutils2.45.1_amd64-kernel-arm_static-20260224.7z"
          for i in 1 2 3; do
            wget -qO clang.7z "$CLANG_URL" && break || { echo "下载失败，重试 $i/3..."; sleep 10; }
          done
          7z x clang.7z -oaosp-clang/

          # 导入环境变量
          echo "${{ github.workspace }}/aosp-clang/bin" >> $GITHUB_PATH

      # ── 4. 准备 AnyKernel3 ──
      - name: 准备 AnyKernel3
        if: inputs.use_anykernel
        run: |
          git clone --recursive --depth=1 -j $(nproc) \
            --branch ${{ env.ANYKERNEL_BRANCH }} \
            ${{ env.ANYKERNEL_REPO }} \
            AnyKernel3

          rm -f AnyKernel3/anykernel.sh
          cat << 'EOF' > AnyKernel3/anykernel.sh
          properties() { '
          kernel.string=Ares Custom Kernel
          do.devicecheck=0
          do.modules=0
          do.systemless=1
          do.cleanup=1
          do.cleanuponabort=0
          '; }
          
          BLOCK=auto;
          IS_SLOT_DEVICE=auto;
          RAMDISK_COMPRESSION=auto;
          PATCH_VBMETA_FLAG=auto;
          
          . tools/ak3-core.sh;
          
          split_boot;
          flash_boot;
          EOF

      # ── 5. 配置并恢复 ccache ──
      - name: 配置 ccache
        if: inputs.ccache_enabled
        run: |
          ccache -o compression=true -o cache_dir=$HOME/.ccache -o max_size=4G
          echo "CCACHE_DIR=$HOME/.ccache" >> $GITHUB_ENV

      - name: 载入 ccache 缓存
        if: inputs.ccache_enabled && !inputs.ccache_update
        uses: actions/cache/restore@v4
        id: ccache_restore
        with:
          path: ~/.ccache
          key: ${{ env.CCACHE_KEY }}-${{ runner.os }}-${{ github.ref_name }}-${{ github.run_id }}
          restore-keys: |
            ${{ env.CCACHE_KEY }}-${{ runner.os }}-${{ github.ref_name }}-
            ${{ env.CCACHE_KEY }}-${{ runner.os }}-
            ${{ env.CCACHE_KEY }}-

      - name: 清除旧 ccache 缓存
        if: inputs.ccache_enabled && inputs.ccache_update
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "正在清除仓库中的旧 ccache 缓存..."
          PREFIX="${{ env.CCACHE_KEY }}-${{ runner.os }}-${{ github.ref_name }}-"
          mapfile -t KEYS < <(gh cache list -R ${{ github.repository }} \
            --key "$PREFIX" --limit 100 --json key --jq '.[].key')
          if [ ${#KEYS[@]} -eq 0 ]; then
            echo "未找到匹配前缀的旧缓存（前缀: $PREFIX）。"
          else
            for k in "${KEYS[@]}"; do
              if gh cache delete "$k" -R ${{ github.repository }}; then
                echo "成功删除旧缓存: $k"
              else
                echo "删除失败或已被清理: $k"
              fi
            done
          fi

      # ── 6. 集成 ReSukiSU ──
      - name: 集成 ReSukiSU (Manual Hook)
        if: inputs.ksu_enabled
        working-directory: ${{ env.KERNEL_NAME }}
        run: |
          rm -rf "./KernelSU" "./drivers/kernelsu"

          curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/main/kernel/setup.sh" | bash -s main

          DEFCONFIG="arch/${{ env.ARCH }}/configs/${{ env.DEFCONFIG_NAME }}"
          {
            echo "CONFIG_KSU=y"
            echo "CONFIG_KSU_MANUAL_HOOK=y"
            echo "CONFIG_KSU_MANUAL_HOOK_AUTO_INPUT_HOOK=y"
            echo "CONFIG_KSU_MANUAL_HOOK_AUTO_SETUID_HOOK=y"
            echo "CONFIG_KSU_MANUAL_HOOK_AUTO_INITRC_HOOK=y"
            echo "CONFIG_KSU_MULTI_MANAGER_SUPPORT=y"
            echo "CONFIG_KALLSYMS=y"
            echo "CONFIG_KALLSYMS_ALL=y"
            echo "CONFIG_KALLSYMS_ABSOLUTE_PERCPU=y"
          } >> "$DEFCONFIG"

      # ── 6.5 集成 Droidspaces 支持 ──
      - name: 集成 Droidspaces 支持
        if: inputs.droidspaces_enabled
        working-directory: ${{ env.KERNEL_NAME }}
        run: |
          set -euo pipefail

          PATCH_SRC="${{ github.workspace }}/droidspaces-src"
          rm -rf "$PATCH_SRC"
          git clone --depth=1 --filter=blob:none --sparse \
            https://github.com/seriaTvT/Droidspaces-OSS "$PATCH_SRC"
          git -C "$PATCH_SRC" sparse-checkout set \
            Documentation/resources/kernel-patches/non-GKI
          NON_GKI="$PATCH_SRC/Documentation/resources/kernel-patches/non-GKI"

          shopt -s nullglob
          for patch in "$NON_GKI"/*.patch; do
            name="$(basename "$patch")"
            mapfile -t targets < <(grep '^+++ ' "$patch" | sed -E 's#^\+\+\+ b/##; s/\t.*$//')

            if git apply --reverse --check "$patch" >/dev/null 2>&1; then
              echo "✅ 已应用，跳过: $name"
              continue
            fi

            if git apply --check "$patch" >/dev/null 2>&1; then
              git apply "$patch"
              echo "✅ 应用成功: $name"
              continue
            fi

            missing_all=true
            for t in "${targets[@]}"; do
              [ -e "$t" ] && missing_all=false
            done
            if [ "$missing_all" = true ]; then
              echo "⚠️  目标文件不存在，跳过不适用补丁: $name (${targets[*]:-?})"
              continue
            fi

            echo "::error::补丁无法应用（疑似源码冲突，请检查上游变更）: $name"
            exit 1
          done

          DEFCONFIG="arch/${{ env.ARCH }}/configs/${{ env.DEFCONFIG_NAME }}"
          if grep -q "Droidspaces support (non-GKI)" "$DEFCONFIG"; then
            echo "Droidspaces 内核配置已存在，跳过追加"
          else
            cat >> "$DEFCONFIG" << 'EOF'

          # ===== Droidspaces support (non-GKI) =====
          CONFIG_FHANDLE=y
          CONFIG_SYSCTL=y
          CONFIG_SYSVIPC=y
          CONFIG_POSIX_MQUEUE=y
          CONFIG_NAMESPACES=y
          CONFIG_PID_NS=y
          CONFIG_UTS_NS=y
          CONFIG_IPC_NS=y
          CONFIG_USER_NS=y
          CONFIG_SECCOMP=y
          CONFIG_SECCOMP_FILTER=y
          CONFIG_CGROUPS=y
          CONFIG_CGROUP_DEVICE=y
          CONFIG_CGROUP_PIDS=y
          CONFIG_MEMCG=y
          CONFIG_CGROUP_SCHED=y
          CONFIG_FAIR_GROUP_SCHED=y
          CONFIG_CGROUP_FREEZER=y
          CONFIG_CGROUP_NET_PRIO=y
          CONFIG_DEVTMPFS=y
          CONFIG_OVERLAY_FS=y
          CONFIG_TMPFS_POSIX_ACL=y
          CONFIG_TMPFS_XATTR=y
          CONFIG_FW_LOADER=y
          CONFIG_FW_LOADER_USER_HELPER=y
          CONFIG_FW_LOADER_COMPRESS=y
          CONFIG_NET_NS=y
          CONFIG_VETH=y
          CONFIG_BRIDGE=y
          CONFIG_NETFILTER=y
          CONFIG_BRIDGE_NETFILTER=y
          CONFIG_NETFILTER_ADVANCED=y
          CONFIG_NF_CONNTRACK=y
          CONFIG_IP_NF_IPTABLES=y
          CONFIG_IP_NF_FILTER=y
          CONFIG_NF_NAT=y
          CONFIG_NF_TABLES=y
          CONFIG_IP_NF_TARGET_MASQUERADE=y
          CONFIG_NETFILTER_XT_TARGET_MASQUERADE=y
          CONFIG_NETFILTER_XT_TARGET_TCPMSS=y
          CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
          CONFIG_NF_CONNTRACK_NETLINK=y
          CONFIG_NF_NAT_REDIRECT=y
          CONFIG_IP_ADVANCED_ROUTER=y
          CONFIG_IP_MULTIPLE_TABLES=y
          CONFIG_NF_CONNTRACK_IPV4=y
          CONFIG_NF_NAT_IPV4=y
          CONFIG_IP_NF_NAT=y
          CONFIG_NETFILTER_XT_MATCH_COMMENT=y
          CONFIG_NETFILTER_XT_MATCH_STATE=y
          CONFIG_NETFILTER_XT_MATCH_CONNTRACK=y
          CONFIG_NETFILTER_XT_MATCH_MULTIPORT=y
          CONFIG_NETFILTER_XT_MATCH_HL=y
          CONFIG_NETFILTER_XT_TARGET_REJECT=y
          CONFIG_IP_NF_TARGET_REJECT=y
          CONFIG_NETFILTER_XT_TARGET_LOG=y
          CONFIG_IP_NF_TARGET_ULOG=y
          CONFIG_NETFILTER_XT_MATCH_RECENT=y
          CONFIG_NETFILTER_XT_MATCH_LIMIT=y
          CONFIG_NETFILTER_XT_MATCH_HASHLIMIT=y
          CONFIG_NETFILTER_XT_MATCH_OWNER=y
          CONFIG_NETFILTER_XT_MATCH_PKTTYPE=y
          CONFIG_NETFILTER_XT_MATCH_MARK=y
          CONFIG_NETFILTER_XT_TARGET_MARK=y
          CONFIG_IP_SET=y
          CONFIG_IP_SET_HASH_IP=y
          CONFIG_IP_SET_HASH_NET=y
          CONFIG_NETFILTER_XT_SET=y
          CONFIG_NETFILTER_NETLINK_QUEUE=y
          CONFIG_NETFILTER_NETLINK_LOG=y
          CONFIG_NETFILTER_XT_TARGET_NFLOG=y
          CONFIG_ARM64_VHE=y
          EOF
          fi

      # ── 7. 版本伪装 ──
      - name: 伪装内核版本信息
        if: inputs.fake_enabled
        working-directory: ${{ env.KERNEL_NAME }}
        env:
          IN_BUILD_USER: ${{ inputs.kbuild_build_user }}
          IN_BUILD_HOST: ${{ inputs.kbuild_build_host }}
          IN_BUILD_TIMESTAMP: ${{ inputs.kbuild_build_timestamp }}
          IN_BUILD_VERSION: ${{ inputs.kbuild_build_version }}
        run: |
          if [ -n "$IN_BUILD_USER" ];      then echo "KBUILD_BUILD_USER=$IN_BUILD_USER"           >> "$GITHUB_ENV"; fi
          if [ -n "$IN_BUILD_HOST" ];      then echo "KBUILD_BUILD_HOST=$IN_BUILD_HOST"           >> "$GITHUB_ENV"; fi
          if [ -n "$IN_BUILD_TIMESTAMP" ]; then echo "KBUILD_BUILD_TIMESTAMP=$IN_BUILD_TIMESTAMP" >> "$GITHUB_ENV"; fi
          if [ -n "$IN_BUILD_VERSION" ];   then echo "KBUILD_BUILD_VERSION=$IN_BUILD_VERSION"     >> "$GITHUB_ENV"; fi

          if [ -n "${FAKE_EXTRAVERSION}" ]; then
            ESCAPED_EXTRA=$(printf '%s\n' "${FAKE_EXTRAVERSION}" | sed 's/[&/\\]/\\&/g')
            sed -i "s/^EXTRAVERSION =.*/EXTRAVERSION = ${ESCAPED_EXTRA}/" Makefile
            sed -i '2i exit 0' scripts/setlocalversion
          fi

          if [ -n "${FAKE_COMPILER}" ]; then
            ESCAPED_COMPILER=$(printf '%s\n' "${FAKE_COMPILER}" | sed 's/[&|\\]/\\&/g')
            if ! grep -q '"\$CC_VERSION, \$LD_VERSION"' scripts/mkcompile_h; then
              echo "::error::未在 scripts/mkcompile_h 找到预期的 LINUX_COMPILER 拼接串，伪装编译器失败"
              exit 1
            fi
            sed -i "s|\"\$CC_VERSION, \$LD_VERSION\"|\"${ESCAPED_COMPILER}\"|" scripts/mkcompile_h
          fi

      # ── 8. 编译内核 ──
      - name: 生成 defconfig
        working-directory: ${{ env.KERNEL_NAME }}
        run: |
          mkdir -p "${{ env.OUT_DIR }}"

          if [[ "${{ inputs.ccache_enabled }}" == "true" ]]; then
            CC_CMD="ccache clang"
          else
            CC_CMD="clang"
          fi

          if [[ "${{ inputs.fake_enabled }}" != "true" && -n "${{ inputs.kernel_suffix }}" ]]; then
            export LOCALVERSION="-${{ inputs.kernel_suffix }}"
          fi

          make -j$(nproc) \
            O=${{ env.OUT_DIR }} \
            ARCH=${{ env.ARCH }} \
            CC="${CC_CMD}" \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            LD=ld.lld \
            AR=llvm-ar \
            NM=llvm-nm \
            OBJCOPY=llvm-objcopy \
            OBJDUMP=llvm-objdump \
            READELF=llvm-readelf \
            STRIP=llvm-strip \
            OBJSIZE=llvm-size \
            KCFLAGS="-Wno-error" \
            KCPPFLAGS="-Wno-error" \
            ${{ env.DEFCONFIG_NAME }}

      - name: 编译内核与设备树
        working-directory: ${{ env.KERNEL_NAME }}
        run: |
          if [[ "${{ inputs.ccache_enabled }}" == "true" ]]; then
            CC_CMD="ccache clang"
            ccache -z
          else
            CC_CMD="clang"
          fi

          if [[ "${{ inputs.fake_enabled }}" != "true" && -n "${{ inputs.kernel_suffix }}" ]]; then
            export LOCALVERSION="-${{ inputs.kernel_suffix }}"
          fi

          make -j$(nproc) \
            O=${{ env.OUT_DIR }} \
            ARCH=${{ env.ARCH }} \
            CC="${CC_CMD}" \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            LD=ld.lld \
            AR=llvm-ar \
            NM=llvm-nm \
            OBJCOPY=llvm-objcopy \
            OBJDUMP=llvm-objdump \
            READELF=llvm-readelf \
            STRIP=llvm-strip \
            OBJSIZE=llvm-size \
            KCFLAGS="-Wno-error" \
            KCPPFLAGS="-Wno-error" \
            Image dtbs

      - name: 输出 ccache 统计
        if: inputs.ccache_enabled && always()
        run: ccache -s

      # ── 9. 保存 ccache ──
      - name: 保存 ccache 缓存
        if: inputs.ccache_enabled
        uses: actions/cache/save@v4
        with:
          path: ~/.ccache
          key: ${{ env.CCACHE_KEY }}-${{ runner.os }}-${{ github.ref_name }}-${{ github.run_id }}

      # ── 11. 准备产物 ──
      - name: 校验内核与设备树镜像
        id: prepare_image
        run: |
          BOOT_DIR="${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot"
          BUILDDATE="${{ steps.setup_env.outputs.builddate }}"
          SUFFIX="${{ inputs.kernel_suffix }}"
          SUFFIX_STR="${SUFFIX:+-${SUFFIX}}"
          IMAGE_NAME="Image-${{ env.KERNEL_DEVICE }}-${{ env.KERNEL_NAME }}${SUFFIX_STR}-${BUILDDATE}"
          DTB_NAME="dtb-${{ env.KERNEL_DEVICE }}-${{ env.KERNEL_NAME }}${SUFFIX_STR}-${BUILDDATE}"
          echo "image_name=${IMAGE_NAME}" >> $GITHUB_OUTPUT
          echo "dtb_name=${DTB_NAME}" >> $GITHUB_OUTPUT

          if [[ ! -f "${BOOT_DIR}/Image" ]]; then
            echo "::error::未找到内核镜像文件 (Image)，构建失败"
            exit 1
          fi
          echo "内核镜像校验通过: ${BOOT_DIR}/Image"

          find "${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot/dts/" -name "*.dtb" -type f | sort | xargs cat > "${BOOT_DIR}/dtb"

          if [[ ! -s "${BOOT_DIR}/dtb" ]]; then
            echo "::error::未找到编译出的 DTB 文件，构建失败"
            exit 1
          fi
          echo "DTB 文件校验及合并通过: ${BOOT_DIR}/dtb"

      - name: 打包 AnyKernel3
        if: inputs.use_anykernel
        id: pack_anykernel
        run: |
          BOOT_DIR="${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot"
          BUILDDATE="${{ steps.setup_env.outputs.builddate }}"
          SUFFIX="${{ inputs.kernel_suffix }}"
          SUFFIX_STR="${SUFFIX:+-${SUFFIX}}"
          AK3_NAME="${{ env.KERNEL_DEVICE }}-${{ env.KERNEL_NAME }}${SUFFIX_STR}_${BUILDDATE}"

          if [ -f "${BOOT_DIR}/Image" ]; then cp -f "${BOOT_DIR}/Image" ./AnyKernel3/; fi
          if [ -f "${BOOT_DIR}/Image.gz" ]; then cp -f "${BOOT_DIR}/Image.gz" ./AnyKernel3/; fi
          if [ -f "${BOOT_DIR}/dtb" ]; then cp -f "${BOOT_DIR}/dtb" ./AnyKernel3/; fi
          if [ -f "${BOOT_DIR}/dtbo" ]; then cp -f "${BOOT_DIR}/dtbo" ./AnyKernel3/; fi
          if [ -f "${BOOT_DIR}/dtbo.img" ]; then cp -f "${BOOT_DIR}/dtbo.img" ./AnyKernel3/; fi

          cd AnyKernel3/
          zip -q -r "${{ github.workspace }}/${AK3_NAME}.zip" *
          echo "ak3name=${AK3_NAME}.zip" >> $GITHUB_OUTPUT

      # ── 12. 上传产物 ──
      - name: 上传 AnyKernel3 刷入包
        if: inputs.use_anykernel
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.pack_anykernel.outputs.ak3name }}
          path: ${{ github.workspace }}/${{ steps.pack_anykernel.outputs.ak3name }}
          if-no-files-found: error
          retention-days: 7

      - name: 上传 Image
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.prepare_image.outputs.image_name }}
          path: ${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot/Image
          if-no-files-found: error
          retention-days: 7

      - name: 上传 dtb
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.prepare_image.outputs.dtb_name }}
          path: ${{ env.OUT_DIR }}/arch/${{ env.ARCH }}/boot/dtb
          if-no-files-found: error
          retention-days: 7

  # ============================================================
  #  发布 Release
  # ============================================================
  release:
    name: 发布 Release
    if: inputs.release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read

    steps:
      - name: 下载构建产物
        uses: actions/download-artifact@v4
        with:
          path: ./release_files

      - name: 设置发布环境变量
        run: |
          TIME="$(TZ='Asia/Shanghai' date +'%y%m%d%H%M%S')"
          TIME_FORM="$(TZ='Asia/Shanghai' date +'%Y-%m-%d %H:%M:%S')"
          TAG_HEAD="ares-build"

          if [[ -n "${{ inputs.kernel_suffix }}" ]]; then
            FULL_VERSION="${{ env.KERNEL_NAME }}-${{ inputs.kernel_suffix }}"
          else
            FULL_VERSION="${{ env.KERNEL_NAME }}"
          fi

          if [[ "${{ inputs.ksu_enabled }}" == "true" ]]; then
            KSU_STATUS="ReSukiSU (Manual Hook)"
          else
            KSU_STATUS="未集成"
          fi

          if [[ "${{ inputs.droidspaces_enabled }}" == "true" ]]; then
            DS_STATUS="已集成 (官方补丁 + 内核配置)"
          else
            DS_STATUS="未集成"
          fi

          if [[ "${{ inputs.fake_enabled }}" == "true" ]]; then
            FAKE_INFO="${{ inputs.kbuild_build_user }}@${{ inputs.kbuild_build_host }}"
            FAKE_TIME="${{ inputs.kbuild_build_timestamp }}"
            FAKE_VER="${{ inputs.kbuild_build_version }}"
          else
            FAKE_INFO="未伪装（真实环境）"
            FAKE_TIME="未伪装（真实环境）"
            FAKE_VER="未伪装（真实环境）"
          fi

          echo "TIME=$TIME" >> $GITHUB_ENV
          echo "TIME_FORM=$TIME_FORM" >> $GITHUB_ENV
          echo "TAG_HEAD=$TAG_HEAD" >> $GITHUB_ENV
          echo "FULL_VERSION=$FULL_VERSION" >> $GITHUB_ENV
          echo "KSU_STATUS=$KSU_STATUS" >> $GITHUB_ENV
          echo "DS_STATUS=$DS_STATUS" >> $GITHUB_ENV
          echo "FAKE_INFO=$FAKE_INFO" >> $GITHUB_ENV
          echo "FAKE_TIME=$FAKE_TIME" >> $GITHUB_ENV
          echo "FAKE_VER=$FAKE_VER" >> $GITHUB_ENV

      - name: 创建 Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cat << 'EOF' > release_notes.md
          ### 构建信息
          - 设备: ares (Redmi K40 Gaming)
          - 内核分支: ${{ env.KERNEL_BRANCH }}
          - 内核版本: ${{ env.FULL_VERSION }}
          - 编译时间: ${{ env.TIME_FORM }}
          - 工具链: Mandi-Sa LLVM 23.0.0 (Binutils 2.45.1)
          - KernelSU: ${{ env.KSU_STATUS }}
          - Droidspaces: ${{ env.DS_STATUS }}
          - AnyKernel3 打包: ${{ inputs.use_anykernel }}
          - CCache 加速: ${{ inputs.ccache_enabled }}
          - 伪装启用: ${{ inputs.fake_enabled }}
          - 伪装信息: ${{ env.FAKE_INFO }}
          - 伪装时间: ${{ env.FAKE_TIME }}
          - 伪装版本: ${{ env.FAKE_VER }}
          EOF

          shopt -s nullglob
          RELEASE_FILES=()

          for f in ./release_files/*/*.zip; do RELEASE_FILES+=("$f"); done
          for f in ./release_files/*/Image; do RELEASE_FILES+=("$f"); done
          for f in ./release_files/*/dtb; do RELEASE_FILES+=("$f"); done

          if [ ${#RELEASE_FILES[@]} -eq 0 ]; then
            echo "::error::未找到任何可发布的文件"
            exit 1
          fi

          gh release create "${{ env.TAG_HEAD }}-${{ env.TIME }}" \
            --repo "${{ github.repository }}" \
            --title "${{ env.FULL_VERSION }}-${{ env.TIME }}" \
            --notes-file release_notes.md \
            "${RELEASE_FILES[@]}"
